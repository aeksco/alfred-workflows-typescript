{
  "name": "alfred-workflows-typescript",
  "description": "Run Node.js + TypeScript scripts through an Alfred workflow",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "dev": "tsc -w -p tsconfig.json",
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf ./dist",
    "compile": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "node-notifier": "^8.0.0",
    "typescript": "^4.1.2"
  },
  "devDependencies": {
    "@types/node": "^14.14.9"
  }
}
